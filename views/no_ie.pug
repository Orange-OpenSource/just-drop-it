extends layout.pug
block extra_scripts
    script.
        var stayOnPage = false;
    | <!--[if IE]> <script>stayOnPage = true;</script><![endif]-->
    script.
        if(!stayOnPage)window.location = "/";

block content
    div.row
        div.col-xs-12.alert.h3
            span.icon-Warningimportant(aria-hidden="true")
                span.sr-only.lead Error
                | This project hasn't been tested on IE. Please use one of the following browser:
                ul
                    li Firefox
                    li Chrome
                    li Safari
